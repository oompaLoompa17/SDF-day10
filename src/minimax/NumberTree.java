package minimax;
import java.io.*;


public class NumberTree {

    public static void main(String[] args) {
        Console cons = System.console();
        String[] numbers = cons.readLine().split(",");
        int[] numbahs = new int[numbers.length];
        for (int i=0; i < numbers.length; i++){
            numbahs[i] = Integer.parseInt(numbers[i]);
        }
       
    }

    // public static StringBuilder recursion(int[] numbahs){
    //       StringBuilder output;
    // }

    public int minimax(int currentDepth, boolean isMax, int values[], int currentNodeIndex, int depth){
        if ( )
        if (isMax){
        
        } else {

        }

        return 0;
    }
}
